/*const input = $(`#input`);

function buscar(texto) {
    texto = input.value;
    productos.forEach((dato) => {
        if (dato.nombre.toLowerCase().includes(texto.toLowerCase())){
            htmlProductos(texto);
        }
    });
}


$(`#boton`).click(buscar());*/

